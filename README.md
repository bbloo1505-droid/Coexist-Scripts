# Mary Cairncross speech (presenter)

Standalone mobile-first cue cards for the Co-Exist / Mary Cairncross nature walk talk.

## Run locally

```bash
npm install
npm run dev
```

Opens on port **5174** by default (so it does not clash with PelviLog on 8080).

## Build

```bash
npm run build
npm run preview
```
